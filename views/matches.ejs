<%- include('partials/header') %>
<% if (success) { %><div class="alert alert-success"><%= success %></div><% } %>
<% if (error) { %><div class="alert alert-error"><%= error %></div><% } %>

<div class="page-header">
  <h1>🤝 Meus Matches</h1>
</div>

<% if (matches.length === 0) { %>
  <div class="empty-page">
    <p>Você ainda não tem matches.</p>
    <p>Navegue pelas <a href="/needs">necessidades abertas</a> e ofereça ajuda!</p>
  </div>
<% } else { %>
  <div class="matches-list">
    <% matches.forEach(match => { %>
      <div class="match-card">
        <div class="match-card-header">
          <span class="tag tag-<%= match.need_category %>"><%= match.need_category %></span>
          <span class="tag tag-<%= match.status %>"><%= match.status %></span>
        </div>
        <h3><%= match.need_title %></h3>
        <p><%= match.need_description ? match.need_description.substring(0, 100) + (match.need_description.length > 100 ? '...' : '') : '' %></p>
        <div class="match-card-meta">
          <% if (match.status === 'accepted') { %>
            <span>Solicitante: <%= match.requester_name || 'Membro do CLOC' %></span>
          <% } else { %>
            <span>Solicitante: anônimo 🔒</span>
          <% } %>
        </div>
        <div class="match-card-actions">
          <% if (match.status === 'pending') { %>
            <span class="tag tag-pending">⏳ Aguardando o solicitante aceitar</span>
          <% } else if (match.status === 'accepted') { %>
            <span class="tag tag-green">✅ Aceito</span>
            <% if (match.requester_whatsapp) { %>
              <a class="btn btn-sm btn-primary" target="_blank" rel="noopener" href="<%= waLink(match.requester_whatsapp) %>">💬 Falar no WhatsApp</a>
            <% } %>
          <% } else { %>
            <span class="tag tag-gray">Recusado</span>
          <% } %>
        </div>
      </div>
    <% }) %>
  </div>
<% } %>
<%- include('partials/footer') %>
