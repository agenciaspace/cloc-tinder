# Dependências
node_modules/

# Variáveis de ambiente (NUNCA comitar — contém segredos)
.env
.env.local
.env.*.local

# Banco local (sql.js) — legado, não usado em produção
data/*.db
data/

# Artefatos
.playwright-cli/
*.log
.DS_Store
.vercel
